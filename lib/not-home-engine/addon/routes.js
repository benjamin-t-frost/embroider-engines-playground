import buildRoutes from 'ember-engines/routes';
import lodash from 'lodash';

// Same bundle as faker in other engine
console.log(lodash);

export default buildRoutes(function () {
  // Define your engine's route map here
});
