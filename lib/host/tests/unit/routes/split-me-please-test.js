import { module, test } from 'qunit';
import { setupTest } from 'host/tests/helpers';

module('Unit | Route | split-route-one', function () {
  // setupTest(hooks);
  // test('it exists', function (assert) {
  //   let route = this.owner.lookup('route:split-route-one');
  //   assert.ok(route);
  // });
});
