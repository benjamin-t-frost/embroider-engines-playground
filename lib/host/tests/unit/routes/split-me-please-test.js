import { module, test } from 'qunit';
import { setupTest } from 'host/tests/helpers';

module('Unit | Route | split-me-please', function () {
  // setupTest(hooks);
  // test('it exists', function (assert) {
  //   let route = this.owner.lookup('route:split-me-please');
  //   assert.ok(route);
  // });
});
